// document.addEventListener("click", (event) => {
//     const target = event.target as HTMLElement;
  
//     // Check if the clicked element has the class "adicionarMapa"
//     if (target.classList.contains("adicionarMapa")) {
//       const benchmarkId = target.getAttribute("data-benchmark-id");
  
//       // Check if benchmarkId exists before sending it to the backend
//       if (benchmarkId) {
//         console.log("Benchmark ID selected:", benchmarkId);
  
//         associateUserBenchmark(benchmarkId);
//       }
//     }
//   });
  

// //   JUNTAR FRONT COM BACK PARA SALVAR PONTOS CADASTRADOS AQUI